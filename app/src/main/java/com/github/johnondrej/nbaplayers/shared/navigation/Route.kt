package com.github.johnondrej.nbaplayers.shared.navigation

enum class Route(val routeDefinition: String) {

    PLAYER_LIST(routeDefinition = "playerList"),
    PLAYER_DETAIL(routeDefinition = "playerDetail"),
    TEAM_DETAIL(routeDefinition = "teamDetail")
}
